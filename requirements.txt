# Core dependencies for CI-AI
# These can be expanded as the project grows

# For JSON and data handling
pandas>=2.0.0

# For training and model pipelines (stubs now, real later)
torch>=2.0.0
transformers>=4.30.0

# For evaluation and utilities
scikit-learn>=1.3.0

# For diagrams (optional, used for generating flowcharts)
graphviz>=0.20.0

# For running scripts safely
click>=8.1.0
